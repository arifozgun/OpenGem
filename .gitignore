node_modules
dist
.next
out
.agents
project_preview
.claude

# TypeScript incremental build cache
*.tsbuildinfo

# Keep environment variables out of version control
.env
**/.DS_Store
# Setup configuration (contains Firebase keys, admin credentials)
config.json
# Local database (contains tokens and user data — never commit)
data/
